# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Translations
*.mo
*.pot

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store

# Jupyter Notebook
.ipynb_checkpoints

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Ruff
.ruff_cache/

# Local development
*.local
.envrc

# twscrape Twitter account database
accounts.db

# 1code
.1code/

# Claude Code
CLAUDE.md

# Web UI toolchain (the build output under
# src/video_searching_agent/web/static is committed on purpose, so the UI
# works from a clean clone without npm)
ui/node_modules/
ui/.vite/
npm-debug.log*
.vercel

# Eval run records and their scorecards. A run is a measurement of one
# deployment at one moment, and it can be large; eval/sample-scorecard.md is
# committed instead so the output's shape is visible from a clean clone.
eval/results/

# The annotation store is data, not code. Its rows come from runs.
data/

# `vercel link` writes an .env.local holding the project's OIDC token, and a
# deployment's own pull writes further .env.* files. None of them belong in the
# repository; .env.example does.
.env*
!.env.example
